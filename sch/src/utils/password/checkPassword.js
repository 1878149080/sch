/**
 * @desc 密码强度判断
 * 1)包含有字母、数字、特殊字符。
 * 2)长度必须大于等于8位。
 * */

// 密码是否符合强度要求
export function checkPassword(text = '') {
  const reg =
    /^(?=.*[A-Za-z])(?=.*\d)(?=.*[`~!@#$%^&*()_+<>?:"{},.\/\\;'[\]])[A-Za-z\d`~!@#$%^&*()_+<>?:"{},.\/\\;'[\]]{8,}$/;
  return reg.test(text);
}
